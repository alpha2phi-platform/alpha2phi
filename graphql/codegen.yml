overwrite: true
schema: "graphql/*.gql"
generates:
  frontend/src/data/urql.ts:
    documents:
      - frontend/src/data/operations.gql
    config:
      withHooks: true
    plugins:
      - typescript
      - typescript-operations
      - typescript-urql
